<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.leandro.controlpensiones">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.BLUETOOTH_PRIVILEGED"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:label="Santafe Pensiones">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".VerificacionRut"
            android:label="Santafe Pensiones">
        </activity>
        <activity android:name=".Splashverificacion"
            android:label="Santafe Pensiones">

        </activity>
        <activity android:name=".CSetting"
            android:theme="@style/DialogTheme">

        </activity>
        <activity android:name=".Ajustes"
        android:label="Santafe Pensiones">

    </activity>
        <activity android:name=".Main_Activity"
            android:label="Santafe Pensiones">

        </activity>
        <activity android:name=".DeviceListActivity"
            android:label="Santafe Pensiones">

        </activity>
    </application>

</manifest>